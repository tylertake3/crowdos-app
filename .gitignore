# dependencies
/node_modules

# next.js build output
/.next
/out

# production
/build

# environment secrets — never commit these
.env
.env.local
.env*.local

# misc
.DS_Store
*.pem
npm-debug.log*

# typescript
*.tsbuildinfo
next-env.d.ts

# eslint's local result cache (only created if you pass --cache)
.eslintcache

# vercel
.vercel

# local check-builds (keeps dev server cache intact)
.next-check

# database backups — never commit (contain all production data)
backups/
public/audit-tmp/
